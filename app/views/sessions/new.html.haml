-provide(:title, "ログイン")
.input-container
  .row
    .col.form-box
      %h1.input-title ログイン
      = form_with(scope: :session, url: login_path, local: true) do |f|
        .form-group
          =f.text_field :email, class: "form-control", placeholder: "email"
        .form-group
          =f.text_field :password, class: "form-control", placeholder: "password"
        .form-group
          =f.check_box :remember_me, class: "form-check"
          =f.label :remember_me, "次回からログインを省略する", class: "form-check-label"
        =f.submit "ログイン", class:"btn btn-info submit-btn"

      .account-container
        %p.do-not-have-account まだアカウントをお持ちでないですか？
        = link_to "アカウント作成", signup_path, class: "btn btn-secondary"

      .account-container
        %p.forget-email パスワードをお忘れですか？
        = link_to "パスワード再設定", new_password_reset_path, class: "btn btn-secondary"