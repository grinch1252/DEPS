%li.user-picture{:id => "user-#{user.id}"}
  = link_to picture_for(user, size: 50), user
  %span.user= link_to user.name, user, class: "user-name"
  -# - if current_user.admin? && !current_user?(user)
  -#   =link_to "delete", user, method: :delete, data: { confirm: "You sure?" }
