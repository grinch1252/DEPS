- if current_user == @user
  .user_image= link_to picture_for_user_info(current_user, size: 50), current_user
- else
  .user_image= link_to picture_for_user_info(@user, size: 50), @user
%h1.user-left-name= @user.name
%span#log-count= pluralize(@user.microposts.count, "log")